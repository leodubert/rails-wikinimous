<h1>Here is the task you clicked</h1>

<%= @article.title  %> : <%= @article.content %>

<%= link_to "update", "#{@article.id}/edit" %>

<%= link_to "Delete", article_path(@article),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
